<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <!-- this creates on the page a link that links to /articles/:id so that we have it organized !-->
      <!-- it replaces the " a href="/articles/<%= article.id %>" " !-->
      <!-- <a href= article_path(article) %>"> or we can use: !-->
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
